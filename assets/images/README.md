# Imágenes requeridas

Coloca aquí los archivos finales con estos nombres:

- `hero-camion.jpg` (recomendado 1600x1000)
- `contacto-bg.jpg` (recomendado 1920x1080)

Mientras no existan, reemplaza temporalmente los `src` en `index.html` por URLs públicas o súbelas con estos nombres.
